task devJar(type: Jar) {
	classifier = 'dev'
	from sourceSets.main.output
}

artifacts {
	archives devJar
}

repositories {
	maven { url 'https://jitpack.io' }
}

dependencies {
	compile 'com.github.makamys:PlayerAPI:1.4:dev'
	compile 'com.github.makamys:RenderPlayerAPI:1.4:dev'
}
